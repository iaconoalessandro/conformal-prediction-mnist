# requirements.R
# Run once to install the single external dependency needed to reproduce this project.
# The neural network and both conformal prediction procedures are implemented in base R.

install.packages("dslabs")
